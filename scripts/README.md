# VPN Re-Checker Binary

Положи сюда скомпилированный бинарник `vpn_recheck` (Linux amd64).

## Сборка

```bash
cd ../../vpn_recheck
cargo build --release
cp target/release/vpn_recheck ../vpn_recheck_bin/scripts/
```
